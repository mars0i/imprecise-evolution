(jbuild_version 1)

(library 
 ((name        models)
  (public_name models)
  (modules (main wrightfisher setchains tdists tranmats credalsetIO))
  (libraries (batteries core owl parmap utils)) ; ppx_deriving.runtime 
  ;(preprocess (pps (ppx_deriving.std)))
  (ocamlopt_flags (:standard -O3)) ; -dcmm -O3 ; -dcmm dumps intermediate rep
  ))
